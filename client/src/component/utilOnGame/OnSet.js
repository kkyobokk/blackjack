import {useState, useEffect, useContext} from 'react';
import Card, {SetRatio} from '../../util/Card';

export default function OnSet({roomId}) {

    return (
        <div style={{width : "100vw", height : "100vh", position : "fixed"}}>
            
        </div>
    )
}